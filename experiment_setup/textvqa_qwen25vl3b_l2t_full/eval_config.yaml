# Local eval config for the trained experiment checkpoint.
#
# Usage:
#   python -m vlmintune.eval --config experiment_setup/textvqa_qwen25vl3b_l2t_full/eval_config.yaml

model:
  name: "qwen25vl_3b_instruct"

experiment:
  name: "textvqa_qwen25vl3b_l2t_full"
  base_dir: "experiments"

eval:
  source: "trained"
  dataset_name: "lmms-lab/textvqa"
  max_samples: 0
  max_new_tokens: 16
  temperature: 0.0
